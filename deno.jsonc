{
  "tasks": {
    "doctor": "deno run -A ./support/bin/doctor.ts",
    "ingest": "deno run -A ./qcsctl.ts --ic-db ingested/ingestion-center.duckdb --resource-db ingested/resource.sqlite.db --src \"src/**/*.csv\" --diags-xlsx ingested/diagnostics.xlsx --diags-json ingested/diagnostics.json"
  }
}
